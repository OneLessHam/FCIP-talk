<div class="navbar-inner">
	<div class="container">
		<a class="brand" href="/">WeakPoint!</a>
		<ul class="nav">
			<li class="dropdown">
				<a href="#" class="dropdown-toggle" data-toggle="dropdown">
					<span class="caret"></span>
				</a>
				<ul class="dropdown-menu">
					{{#each topics}}
						<li><a href="#" class="selectTopic">{{this}}</a></li>
					{{/each}}
				</ul>
			</li>
		</ul>
		<a class="brand currentTitle" href="/">{{ title }}</a>
		<ul class="nav pull-right">
			{{#each items}}
				<li>
					<a href="{{href}}" class="internalLink">{{title}}</a>
				</li>
			{{/each}}
		</ul>
	</div>
</div>
